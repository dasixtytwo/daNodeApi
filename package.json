{
	"name": "api",
	"version": "0.1.0",
	"description": "New Personal Api",
	"main": "server.js",
	"engines": {
		"npm": "6.11.3",
		"node": "12.10.0"
	},
	"scripts": {
		"start": "node server.js",
		"heroku-postbuild": "npm install --only=dev && npm install && npm run build",
		"post-build": "npm install --only=dev && npm install && npm run build"
	},
	"author": "Davide Agosti",
	"license": "MIT",
	"dependencies": {
		"bcryptjs": "^2.4.3",
		"body-parser": "^1.19",
		"express": "^4.17.1",
		"jsonwebtoken": "^8.5.1",
		"lodash": "^4.17.15",
		"mongoose": "^5.7.5"
	}
}
